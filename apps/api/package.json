{
  "name": "@domus/api",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "node dev.js",
    "build": "python -m compileall backend",
    "lint": "ruff check . && black --check .",
    "test": "pytest"
  }
}
