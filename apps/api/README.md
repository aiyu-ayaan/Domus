# Domus API

FastAPI backend for Domus.

## Run

1. Create a Python 3.12 virtual environment.
2. Install the package in editable mode with dev extras.
3. Run `uvicorn backend.main:app --reload` from `apps/api`.
