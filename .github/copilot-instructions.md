# Domus Workspace Instructions

- Keep changes focused on the Domus smart-home platform.
- Preserve the modular monolith boundaries between `apps/web`, `apps/api`, and `packages/*`.
- Prefer typed, small, composable modules.
- Route all device control through integration adapters.
- Keep the UI polished, responsive, and home-automation focused.
